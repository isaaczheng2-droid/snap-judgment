# Append-only ledgers written by both the hourly rebuild and the weekly learning cycle:
# a concurrent commit must never turn into a merge conflict, only a union of lines.
learn/data/*.ndjson merge=union
live/data/*.ndjson merge=union
